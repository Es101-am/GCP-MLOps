# Kubeflow Pipelines SDK (latest stable v2)
kfp==2.7.0
kfp-pipeline-spec==0.3.0
kfp-server-api==2.0.5

# Vertex AI SDK (keep at least 1.56+ for compatibility with KFP v2)
google-cloud-aiplatform==1.56.0

# Supporting Google libraries (fixes protobuf & API core version conflicts)
google-cloud-storage>=2.14.0,<3
google-api-core>=2.11.0,<3
protobuf>=4.23.0,<5

# (optional, safe utilities often used in pipelines)
pandas>=2.0.0
scikit-learn>=1.2.0
xgboost>=1.7.0
joblib>=1.3.0
